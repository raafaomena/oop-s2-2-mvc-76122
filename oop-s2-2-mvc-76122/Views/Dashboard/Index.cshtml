@model List<oop_s2_2_mvc_76122.Models.Inspection>

<h2>Dashboard</h2>

<p>Overdue FollowUps: @ViewBag.Overdue</p>

<table class="table">
    <tr>
        <th>Premises</th>
        <th>Date</th>
        <th>Score</th>
        <th>Outcome</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Premises?.Name</td>
            <td>@item.InspectionDate</td>
            <td>@item.Score</td>
            <td>@item.Outcome</td>
        </tr>
    }
</table>